Love this ask — you’re basically saying:

> “Give me one OA-level question harder / trickier than standard pattern banks
> But keep the statement short.”

Here’s one that fits perfectly — short to read, brutal to solve, very FAANG-OA style.

---

# 🧠 Question — Dynamic Median Cost

You are given an integer array `nums`.

For every prefix `i` (0 → i), compute the **minimum cost** to make all elements equal.

Cost = sum of absolute differences.

Return an array `cost[]`.

---

## Example

```
nums = [1, 3, 2]
```

Prefixes:

```
[1]       → cost = 0
[1,3]     → make both 2 → cost = 2
[1,3,2]   → make all 2 → cost = 2
```

Output:

```
[0, 2, 2]
```

---

# Why This Is Beyond Standard

It mixes:

* Median property
* Prefix processing
* Two heaps
* Running cost tracking

You cannot brute each prefix → too slow.

---

# Constraints (OA realistic)

```
1 ≤ n ≤ 2*10^5
|nums[i]| ≤ 10^9
```

So brute O(n²) → TLE.

---

# Core Insight (not full solution)

To minimize:

```
Σ |xi − m|
```

Best `m` = median.

So as prefix grows:

* Maintain median dynamically
* Maintain left sum + right sum

Use:

```
MaxHeap (left)
MinHeap (right)
```

Cost formula:

```
median*leftSize − leftSum
+
rightSum − median*rightSize
```

---

# Complexity Target

| Approach        | Time         |
| --------------- | ------------ |
| Brute prefix    | O(n²)        |
| Sort every time | O(n² log n)  |
| Heap median     | O(n log n) ✅ |

Space → O(n)

---
